cmake_minimum_required(VERSION 3.20.0)

# This file is picked up by the ZMK module system.
# When the oled_gifs shield is active the shield directory adds its own
# CMakeLists.txt; nothing else is needed here.
